# AGENTS.md · 给 AI 的协作契约

> 这个文件**只给 AI 看**，不是给人读的项目介绍（那个是 [`README.md`](README.md)）。
> 人类读的部分、AI 读的约定、Agent 干过的活，三者分开存放：
>
> | 给谁看 | 放哪 | 内容 |
> | :--- | :--- | :--- |
> | 人 | `README.md` · `index.html` · `0-记录区/` · `1-知识库/` · `2-面试题库/` | 项目是什么、怎么学、怎么跑 |
> | **AI（本文件）** | `AGENTS.md` | 协作约定、加工规则、流水线、禁区 |
> | 人（回顾用） | `docs/` | Agent 工作记录、进度明细、部署坑 |

---

## 一、仓库在干什么

学习 <https://ai-agent-guide.xiaofuge.cn/>（27 章 AI Agent 教程）的笔记体系。
要解决的不是"记下来"，而是**记得住、面试讲得出来**。

**四层 + 一个工具**，每层职责固定，不串：

| 层 | 目录 | 谁写 | 干什么 |
| :-- | :--- | :--- | :--- |
| ① 采集 | `0-记录区/` | **用户本人**（这是他的地盘，AI 不代写正文） | 边学边记，允许潦草；打记号 |
| ② 沉淀 | `1-知识库/` | **AI 加工** | 按**知识点**重组（不按章节）：30 秒结论 · 机制 · 易错点 · 📌你的原文 · 🔆强化延伸 |
| ③ 冲刺 | `2-面试题库/开放题/` | **AI 加工** | 选择题去选项化（六手法）+ 追问原理 → 开放题口述版，**按知识点一个文件**；`02-开放题-口述版.md` 已降级为入口页（只放分布表与使用节奏） |
| ④ 测验 | `3-测验/` | **AI 维护** | `bank.json` 唯一数据源，`index.html` 生成产物 |
| ⑤ 代码 | `4-code/` | 跟章节配套的实操 demo | 两个互相独立的 demo |

**分层的硬原则**：知识库**按知识点组织，不按章节**。同一知识点跨章出现就合并，不要建重名条目。
新增条目前先查 `1-知识库/README.md` 的总表，确认是否已有落点。

### 每条知识库卡片的固定开头（2026-09-20 起强制执行）

> 背景：单条 300+ 行的长文没人会打开。正文不删，但**降级为参考资料**——真正要读的是顶部这三行。

````markdown
## ⚡ TL;DR（先看这三行，下面正文是参考资料）

- **一句话**：……
- **三要点**：①……　②……　③……
- **追问**：……（→ [O-xx](../2-面试题库/开放题/xx.md)）
````

| 行 | 要求 |
| :--- | :--- |
| **一句话** | 必须是**结论**，不能写成"本文介绍了……" |
| **三要点** | 从同文件的 `## 🎯 30 秒结论` **压**出来，不是另写一套 |
| **追问** | 必须能链到 `开放题/` 里**真实存在**的题；该知识点没题就写"（本条暂无开放题，待补）" |

**开放题的组织方式**：`2-面试题库/开放题/NN-知识点名.md`，文件名的 `NN` 与 `1-知识库/` 条目的编号一一对应；
每篇顶部带「掌握度」勾选（认得 / 能讲 / 能讲+追问）。**O-xx 全局编号不变**，改题不改号。

---

## 二、记号系统（用户在记录区写 → AI 按记号加工）

| 用户写的记号 | 含义 | **AI 在整理阶段做什么** |
| :-- | :--- | :--- |
| `==……==` | 想加强的知识点 | 知识库对应条目加「🔆 强化延伸」+ 派生测验题（`s:1`） |
| `~~A~~ B` | 他选 A（错），答案 B | 面试题库记错题 → 归纳根因 → 写「错题根治」→ 按 id 进测验错题池 |
| 正文摘录 / 八股原文 | 他认为重要的原文 | **保真**收录进知识库「📌 你的原文记录」，**不改写、不润色** |
| `> 疑问：……` | 他自己想到的问题 | 知识库「❓ 你的疑问解答」正面回答 + 配自测题；不要糊弄过去 |

**整理时机**：当天只记不整；一章结束或周末做二次整理。
**整理的本质是合并同类项，不是重抄一遍。**

### 每批整理结束的交付动作

1. 记录区记号逐条落地（上面四行，一条都不能漏）
2. 知识库新增/更新条目 + `1-知识库/README.md` 总表、🔆 清单、❓ 索引同步
3. 面试题库：开放题追加 + `2-面试题库/README.md` 索引同步
4. 测验：`bank.json` 加题 → `npm run sync`
5. `0-记录区/ToPractice.md` 补本批的动手项
6. **在 `docs/worklog/` 追加一份本批工作记录**（模板见 `docs/worklog/README.md`）
7. `docs/progress.md` 更新进度快照

---

## 三、测验题库流水线（改题只改一个文件）

**核心约定：`3-测验/bank.json` 是题库的唯一编辑入口，`3-测验/index.html` 是生成产物。**

```
3-测验/bank.json           ← ★ 唯一编辑入口（含 id / concept / w / s）
        │  node tools/build.js
        ▼
3-测验/index.html          ← 生成产物（题库已内联）★ 不要手改题目
```

```bash
node tools/build.js         # bank.json → 内联进 index.html
node tools/run-tests.js     # 回归测试
npm run sync                # 上面两步一键（build → test）
```

> ⚠️ `tools/export-bank.js` 是**重算权重的覆盖写工具**，日常改题**不需要**它。
> 它会按推导规则重算每题 `w`，手工调过的权重会被抹掉，所以默认**拒绝执行**，必须显式 `--force`
> （`npm run recalc` = export --force → build → test）。只在"要整体重算权重"时才用。

### bank.json 字段

| 字段 | 含义 | 说明 |
| :--- | :--- | :--- |
| `id` | 稳定标识 `chXX-NN` | 错题池按 id 持久化，**增删题不会串号**；已分配的 id 永不复用 |
| `c` | 章节 `ch00`~`ch12` | 组题时按章节统计 |
| `concept` | 知识点标签（已归一化） | 同一组卷内**同 concept 只取一题**（去重开关） |
| `w` | 权重 1~5 | 决定被抽中概率 |
| `t` | 题型 `single` / `multi` | |
| `lv` | 深度 L1~L4 | 对照 `2-面试题库/README.md` 的四级深度 |
| `q` / `o` / `a` / `e` | 题干 / 选项 / 答案 / 解析 | |
| `s` | `1` = 由 `==` 强化的派生题 | 可选 |
| `tag` | 原题标记（`🔆` / `🔁` 等） | 前缀在抽取时已剥离进 `concept` |

### 权重 `w` 推导规则（累加后截断到 1~5）

| 信号 | 加分 |
| :--- | :--- |
| 基础分 | 1 |
| `🔆` 标星（用户标记的强化项） | +2 |
| 命中错题池概念（他答错过） | +2 |
| 多选题 | +1 |
| 深度 L3 | +1 |
| 深度 L4 | +2 |
| 命中开放题概念（OQ 有对应口述题） | +1 |

### 两种部署形态（同一份文件）

| 形态 | 打开方式 | 题库来源 |
| :--- | :--- | :--- |
| 单文件 | 双击 `index.html`（`file://`） | 内联 `BANK_INLINE` |
| 双文件 | 起 http 服务 | `fetch('./bank.json')` |

页面优先用内联数据，内联为空时回退 `fetch`。改题后必须 rebuild，否则单文件形态还是旧题。

---

## 四、命名与编号

| 类型 | 规则 | 例子 |
| :--- | :--- | :--- |
| 章节记录 | `chXX-中文短标题.md` | `ch02-什么是AI-Agent.md` |
| 知识卡 | `K-序号`（全局唯一） | `K-07` Token 密度 |
| 选择题 / 开放题 | `Q-序号` / `O-序号` | `O-03` |
| 知识库条目 | `序号-主题.md` | `02-Token与上下文经济学.md` |

> 编号一旦分配**不再改动**，跨文件引用一律用编号（`见 K-07`、`衍生自 Q-12`）。
> 删内容不回收编号。

---

## 五、禁区（不要做）

| 不要 | 原因 |
| :--- | :--- |
| 手改 `3-测验/index.html` 里的题目 | 它是生成产物，下次 build 会被覆盖 |
| 改写记录区的原文摘录 | 用户明确要求保真收录，改写就失去了"我的原文"的价值 |
| 把知识库按章节组织 | 分层硬原则：按知识点 |
| 写知识库条目时省略 `## ⚡ TL;DR` | 长文没人打开，顶部三行是唯一会被读到的部分 |
| 把新开放题追加回 `02-开放题-口述版.md` | 它已降级为入口页；新题写进 `开放题/` 下对应知识点的文件 |
| 在 Notion 里改题目正文 | Notion 是发布物，下次全量重建会直接冲掉，且本地没有这份改动 |
| 把 Agent 工作记录写进 `README.md` | README 是给人看的入口；工作记录放 `docs/worklog/` |
| 在笔记里新增 Jinja2 / Liquid 语法而不确认 Pages | 会触发 Jekyll 构建失败，见 `docs/deploy-pages.md` |
| 把 token / 凭证写进任何文件 | 云同步 token 只存 localStorage，见 `docs/sync.md` |

---

## 六、其他约定

- **多端同步**：错题池 / 组卷参数 / 答题历史走 GitHub 私密 Gist（浏览器直连，无后端）。
  存储 key：`aag_quiz_wrong_v3` / `aag_quiz_compose_v1` / `aag_quiz_hist_v1`；
  凭证存 `aag_quiz_cs_v1`（不上云）。上云 payload 只含 `{app, version, exportedAt, wrong, compose, hist}`。
- **测试**：`npm run test`。前五套跑在 Node vm 沙箱里**没有 CSS 引擎**，布局问题必须用 `test-layout.js`（真 Chrome）兜底 —— 曾经出现"测试全过、页面塌成 60px 窄竖条"。
- **配套代码**：两个 demo 依赖与跑法都不同，见 `4-code/README.md`。

---

## 七、Notion 同步规则（2026-09-21 定）

### 7.1 总原则：本地按「改」组织，Notion 按「用」组织

> 同步**不是**「目录 → 页面」的镜像，而是「源文件 → 发布形态」的编译。
> 本地拆成 24 个文件，**不等于** Notion 要建 24 个子页。

- **本地 Git = 唯一真源**，Notion 一律视为发布物（只读镜像）。
- 决定 Notion 形态的只有一件事：**这一页你拿来干什么**。

| 内容 | 消费场景 | Notion 发布形态 |
| :--- | :--- | :--- |
| 开放题（58 题） | 每天练、打勾 | **单页**：页顶掌握度总表 + 题面常显、答案 `{toggle="true"}` 折叠 |
| 选择题（119 题） | 查错、对答案 | **单页**：按章分组，题干 + 解析折叠 |
| 知识库（25 条） | 检索 TL;DR | **单页索引**：每条三行 + GitHub 原文链接，**不搬正文** |

- 硬规则：**一个本地目录 = 一个 Notion 页面**。单页内容超过约 3 万字才考虑拆子页。
- 反例（已踩）：把 24 个开放题文件做成 24 个 Notion 子页 → 点来点去，没人会打开。

### 7.2 单向与时机

| 规则 | 说明 |
| :--- | :--- |
| 先 commit 再同步 | 同步时本地必须有可回溯版本，页头要记 commit SHA |
| 一次只推一类 | 开放题 / 选择题 / 知识库分开推，出问题好定位 |
| 不在 Notion 改正文 | 唯一例外是**掌握度勾选**（那是状态，不是内容） |

### 7.3 幂等与版本

- **同步 = 全量重建**。MCP 没有 delete 能力，增量更新只会留下删不掉的垃圾块。
- **目标页 ID 固定写死**，不新建页。旧结构改名 `（归档·勿用）` 并加顶部提示，由用户在 UI 手动删。
- 页头必须写同步标记：

  ```
  > 同步于 2026-09-21 10:00 · 本地 58 题 / 24 组 · 源 commit a1b2c3d
  ```

- **重建前先 fetch 旧页，把掌握度勾选读回来**再写进新内容——状态不能丢。

### 7.4 掌握度的唯一真源

> 拆分留下的缺陷：掌握度同时可勾的位置有 **两处**（`README.md §六·补` 的 24 行表 + `开放题/*.md` 头部各一行），必然漂。

- **掌握度以 Notion 为准**（人在那儿练）。
- 每次同步时：从 Notion 读回勾选 → 覆盖本地 `README.md §六·补` 表 **和** `开放题/*.md` 的 `**掌握度**` 行。
- 本地不要再手勾，改了也会被下次同步覆盖。

### 7.5 写入格式（Notion 专有坑，写入前必做预处理）

| 坑 | 处理 |
| :--- | :--- |
| Markdown 表格被整体转义成纯文本 | 改写成**多行 + Tab 缩进**的 HTML `<table fit-page-width="true" header-row="true">` |
| 多行 `>` 被拆成多个引用块 | 用 `<br>` 合并成单块（空行 → `<br><br>`） |
| Callout 渲染成空壳 | `<callout>` 的子内容必须 **Tab 缩进** |
| `{toggle="true"}` 收不进子内容 | toggle 下的子块必须 **Tab 缩进一层** |
| `N. ` 被拆成「空 to-do + 子编号」 | 写成 `N\. ` |
| `$` 被吞 | JSON 传参里写 `\$` |
| 裸文件名 `AGENTS.md` 被自动补成 `http://AGENTS.md` | 写成行内代码 `` `AGENTS.md` `` |
| 加粗套行内代码 `` **`x`** `` 渲染成 `****` | 避免嵌套，改成 `` `x` `` 或纯加粗 |
| 本地相对链接 | 转成 GitHub blob 绝对 URL（路径段 URL-encode） |
| `replace_content` | **会删子页**，新内容必须带上全部 `<page url=…>` 标签 |

### 7.6 同步后必做复核

`notion-fetch` 目标页，逐项确认：题数（如 O-01~O-58 = 58）、分组数（24）、表格渲染成真表格、
`{toggle}` 标记保留、编号没被拆成 to-do、转义正常、`****` / 假链接已清。
**返回 success ≠ 落库**，长内容必须 fetch 复核。
