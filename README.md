# AI Agent 教程 · 学习笔记

> 📖 **图形化工作台：打开 [`index.html`](index.html)** —— 里面有完整的工作流图、27 章章节地图、选择题→开放题六手法、四级深度表、文件清单。
> 本文件只保留最小必要的说明。

---

## 这是什么

学习 <https://ai-agent-guide.xiaofuge.cn/>（27 章 AI Agent 教程）的笔记体系。
解决的问题：**边学边记容易变成抄书，抄完记不住、面试讲不出来。**

## 三层结构 + 一个测验工具

| 层 | 目录 | 干什么 | 产出 |
| :-- | :--- | :--- | :--- |
| ① 采集 | `0-记录区/` | 边学边记，允许潦草（**你的地盘**） | 正文要点 · 八股原文 · 原题（含我的答案） |
| ② 沉淀 | `1-知识库/` | 按**知识点**重组，不按章节 | 30 秒结论 · 机制 · 易错点 · 📌你的原文 · 🔆强化延伸 |
| ③ 冲刺 | `2-面试题库/` | 选择题去选项化 + 追问原理 | 原题库（备查）+ 开放题口述版（主攻） |
| ④ 测验 | `3-测验/bank.json` + `index.html` | 题库 JSON 是**唯一编辑入口**；HTML 是生成的试卷壳 | `bank.json`（90 题）+ 可随机组题的分页试卷 |
| ⑤ 代码 | `4-代码区/` | 跟章节配套的实操代码 | `first_agent/`：Agent 循环 + 调用链 Tracer + 两个工具 |

## 记号系统（记录区 → 我按记号加工）

| 你在记录区写 | 含义 | 我做什么 |
| :-- | :--- | :--- |
| `==……==` | **加强学习标记** | 知识库加「🔆 强化延伸」+ 派生测验题 |
| `~~A~~ B` | 我选 A（错），答案是 B | 面试题库记错题 → 归纳根因 → 写「错题根治」→ 进测验错题池 |
| 正文摘录 / 八股原文 | 你认为重要的原文 | 保真收录进知识库「📌 你的原文记录」，不改写 |
| `> 疑问：……` | 你想到的问题 | 知识库「❓ 你的疑问解答」正面回答 + 配自测题 |

**整理时机**：当天只记不整；一章结束或周末做二次整理。整理的本质是**合并同类项**，不是重抄。

## 每章照做

1. 复制 `0-记录区/_模板-章节记录.md` → `0-记录区/chXX-标题.md`
2. 读的过程中只填「📖 正文要点」
3. 读完立刻做课后选择题：**只用 `~~X~~ Y` 记错题**（我选 X 错，答案是 Y），对的不用抄
4. 疑问用 `> 疑问：……` 写在对应段落旁边，不现场查
5. **标 `==`**：哪些知识点想加强，用 `==……==` 包住标题或关键词
6. 二次整理：原文归并进知识库；高亮项加 🔆 延伸；错题与衍生题进面试题库
7. 做测验：打开 `3-测验/index.html` → 「🔆 强化专项」→ 再「❌ 错题重做」
8. 复习：30 秒看知识库总表 → 3 分钟出声讲开放题

## 题库流水线（改题只需改一个文件）

**核心约定：`3-测验/bank.json` 是题库的唯一编辑入口，`index.html` 是生成产物。**

```
2-面试题库/01-选择题*.md   ← 原始记录（备查，手工同步源）
        │  node tools/export-bank.js
        ▼
3-测验/bank.json           ← ★ 唯一编辑入口（含 id / concept / w）
        │  node tools/build.js
        ▼
3-测验/index.html          ← 生成产物（题库已内联），不要手改题目
```

### 改题流程

```bash
cd AiAgentGuide
node tools/export-bank.js   # ① 从 01-选择题 重新抽取（会覆盖 bank.json）
#    → 手工编辑 3-测验/bank.json（增删改题、调 tag、调 w）
node tools/build.js         # ② 把 bank.json 内联进 index.html
node tools/run-tests.js     # ③ 45 项回归测试
```

一键等价：`npm run sync`（= export → build → test）。

> ⚠️ `export-bank.js` 是**覆盖写**。若已在 `bank.json` 里手工改过题，先备份再导出，或直接手改 `bank.json` 后跳到 ②③。

### bank.json 字段

| 字段 | 含义 | 说明 |
| :--- | :--- | :--- |
| `id` | 稳定标识 `chXX-NN` | 错题池按 id 持久化，**增删题不会串号** |
| `c` | 章节 `ch00`~`ch03` | 组题时按章节统计 |
| `concept` | 知识点标签（已归一化） | 同一组卷内**同 concept 只取一题**（去重开关） |
| `w` | 权重 1~5 | 决定被抽中的概率，见下表 |
| `t` | 题型 `single` / `multi` | |
| `lv` | 深度 L1~L4 | |
| `q` / `o` / `a` / `e` | 题干 / 选项 / 答案 / 解析 | `s` 为强化派生的补充分支（可选） |
| `tag` | 原题标记（`🔆` / `🔁` 等） | 前缀在抽取时已剥离进 `concept` |

### 权重 `w` 推导规则

从「这题是不是重点」推导，累加后**截断到 1~5**：

| 信号 | 加分 |
| :--- | :--- |
| 基础分 | 1 |
| `🔆` 标星（你标记的强化项） | +2 |
| 命中错题池概念（你答错过） | +2 |
| 多选题（本身就更难） | +1 |
| 深度 L3 | +1 |
| 深度 L4 | +2 |
| 命中开放题概念（OQ 有对应口述题） | +1 |

实测分布：`w=5: 9 题 · w=4: 15 · w=3: 11 · w=2: 31 · w=1: 24`，每章都有 w≥4 的题。

### 组卷参数（HTML 页面工具栏）

| 参数 | 作用 |
| :--- | :--- |
| 题量 | 本次组多少题 |
| 权重 | 按 `w` 加权抽样（关掉=等概率） |
| 去重 | 同一 `concept` 只取一题（关掉=可重复考点） |
| 种子 | 同一种子 → 同一份卷子（可复现）；「换一批」重生种子 |

### 两种部署形态（同一份文件）

| 形态 | 打开方式 | 题库来源 | 适用 |
| :--- | :--- | :--- | :--- |
| **单文件** | 双击 `index.html`（`file://`） | 内联 `BANK_INLINE` | GitHub Pages / 发文件给同学 / 离线 |
| **双文件** | 起 http 服务访问 | `fetch('./bank.json')` | 本地改题即时生效，不用 rebuild |

> 页面优先用内联数据；内联为空时自动回退到 `fetch('./bank.json')`。
> 这样**既能单文件部署，又不牺牲改题效率** —— 直接双击能用，是因为数据已经躺在 HTML 里了。

## 命名与编号

| 类型 | 规则 | 例子 |
| :--- | :--- | :--- |
| 章节记录 | `chXX-中文短标题.md` | `ch02-什么是AI-Agent.md` |
| 知识卡 | `K-序号`（全局唯一） | `K-07` Token 密度 |
| 选择题 / 开放题 | `Q-序号` / `O-序号` | `O-03` |
| 知识库条目 | `序号-主题.md` | `02-Token与上下文经济学.md` |

> 编号一旦分配**不再改动**，跨文件引用一律用编号（如 `见 K-07`、`衍生自 Q-12`）。

## 当前进度

- 已记录：第 0、1、2、3 章
- 知识库：**15 条**（01~16，跳号无 10 之前缺项）—— 01~09 覆盖 Agent 全景 → 思维框架；10~16 覆盖 Agent 定义 / 8步链路与调度 / 权限可逆性 / Agent Loop / 工具 Schema 与异常 / 多工具与成本 / 边界与混合架构
- 面试题库：选择题 **72 道**（ch00 15 + ch01 27 + ch02 15 + ch03 15）、开放题 **30 道**（O-01 ~ O-30）
- 测验：**90 题**（教程原题 72 + 🔆 强化派生 18），已外置为 `3-测验/bank.json`（唯一编辑入口），含错题池（按稳定 id 持久化）
- 测试：`npm run test` → **45 项全绿**（引擎逻辑 25 / 渲染路径 15 / 部署形态 5）
- 配套代码：`4-代码区/first_agent/`（uv + Python 3.12）
- 已解答你的疑问 3 处：位置编码是否多余 / 意图识别要不要拆独立子任务 / 多工具是否真的不用硬编码
- 已根治错题 3 道：余弦相似度值域、128K Token 分配、多工具协作「无需硬编码」

## 原始记录说明

`backup/note1.md`（第 0/1 章的第一版随手记录）内容已拆分吸收进 `0-记录区/ch00-*` 与 `ch01-*`，作为备份保留。
