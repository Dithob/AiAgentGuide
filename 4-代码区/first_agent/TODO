# Agent 多轮会话优化

## 第一优先级：可靠性

- [x] 将 SDK assistant 消息统一转换为普通字典后再加入 `messages`
- [x] 增加 Agent 最大循环次数，避免模型持续调用工具导致死循环
- [x] 捕获工具参数 JSON 解析异常，并将错误作为 `tool` 消息返回模型
- [x] 忽略空输入，避免无意义的模型请求

## 第二优先级：上下文控制

- [ ] 增加最近消息数量或 token 上限
- [ ] 裁剪历史时始终保留 system 消息
- [ ] 对过长的工具结果进行截断或压缩
- [ ] 记录上下文裁剪前后的 token 数量

## 第三优先级：会话记忆

- [ ] 为较早对话生成会话摘要
- [ ] 将会话摘要与最近 N 轮原始消息分开管理
- [ ] 区分当前会话上下文与长期用户记忆
- [ ] 设计会话重置和长期记忆清理机制
