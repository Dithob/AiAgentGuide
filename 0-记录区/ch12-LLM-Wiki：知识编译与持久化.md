# 第12章 LLM-Wiki：知识编译与持久化

> **来源**：<https://ai-agent-guide.xiaofuge.cn/> → 第 12 章
> **记录日期**：2026-09-18　|　**重要度**：★★★　|　**掌握度**：

---

## 📌 记号约定（记录区专用）

| 记号         | 含义                               | 整理阶段我会怎么处理                              |
| :----------- | :--------------------------------- | :------------------------------------------------ |
| `==……==`     | **加强学习标记**——这个知识点要加深 | 知识库加「🔆 强化延伸」+ 派生测验题                |
| `~~A~~ B`    | 我选了 A（错），正确答案是 B       | 记进面试题库错题表 → 写「错题根治」→ 进测验错题池 |
| 原文摘录     | 我认为重要的原文，**原样贴**       | 保真收录进知识库「📌 你的原文记录」，不改写        |
| `> 疑问：……` | 我自己想到的问题                   | 知识库「❓ 你的疑问解答」正面回答 + 配自测题       |

> 记的时候别管格式好不好看。**记号是给整理阶段看的**，不是给别人看的。

---

## 🗺️ 一句话地图

> 2~3 句话说清本章主线：解决什么问题、和上一章什么关系、学完能回答什么。
> 目的是两个月后翻回来 5 秒想起整章。

---

## 📖 正文记录

> 直接从教程**原样摘录**你认为重要的表格、代码、段落。别改写、别总结——改写留给整理阶段。
> 判断标准：面试官问到这个，我答不上来会不会很难看？会 → 摘。

### 什么是 LLM-Wiki

LLM-Wiki 最初由 Andrej Karpathy 在其 [llm-wiki gist](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) 中提出，核心理念是：**用文件系统代替向量检索，用自然语言代替微调，让知识被编译一次、持续维护**。传统 RAG 每次查询都从头检索和回答，而 LLM-Wiki 让 LLM 增量构建并维护一个持久化的知识库——知识只编译一次并持续更新，而非每次查询都重新推导。

#### 💡 一句话定义

LLM-Wiki = 存放在项目目录中的 Markdown 文件体系，Agent 启动时自动读取并注入 System Prompt，让 Agent 无需微调就能"知道"项目的一切。延伸到广义实现，LLM-Wiki 也是一种让 AI 持续构建和维护个人知识库的方法论。



#### 🏗️ 三层架构详解

**Raw Sources（原始资料层）**：用户提供的文档、链接、笔记，存放在 `raw/` 目录，不可变。这是知识的"源头"。

**Wiki（知识页面层）**：LLM 从原始资料中提取、整理、互相链接的知识页面。包含实体页、主题页、资料摘要页。每次有新素材加入，Wiki 增量更新而非重建。

**Schema（规则层）**：定义 Wiki 的结构规则、页面类型、分类方式。Schema 是"元知识"——关于知识本身的知识。

#### 三个核心操作驱动整个系统运转：

- **Ingest（消化）**：新素材进入，LLM 分析并生成 Wiki 页面
- **Query（查询）**：基于已有 Wiki 内容回答问题，而非重新阅读原始文件
- **Lint（检查）**：定期检查 Wiki 健康度——孤立页面、断链、矛盾信息



**LLM-Wiki 适合"规则少但重要"的知识，RAG 适合"量大但需要筛选"的知识**。两者互补，不互斥。在实际工程中，Agent 项目通常组合使用：AGENTS.md 放核心规则，RAG 放海量文档，LLM-Wiki 知识库管理系统放整理过的知识体系。



### 两种实现形态：Agent 内嵌式与独立知识系统

Karpathy 的原始设计本质上是一种**知识组织与 Agent 使用模式**：将结构化知识以文件形式交给 LLM，由 Agent 在任务过程中检索、阅读和持续维护。

在实际工程中，这一理念主要演化出了两种实现路径：

#### 形态一：Agent 内嵌式

将知识库直接放入 Agent 的工作环境中，以 Markdown、Skill 或项目级知识文件的形式存在，由 Agent 自己完成读取、检索和维护。

这种模式的核心特点是：

- **轻量**：通常只需要 Markdown 文件和目录结构，不依赖独立数据库或后端服务
- **Agent 原生**：知识与代码、任务上下文处于同一个工作空间，Agent 可以直接读写
- **易于迁移**：知识层与具体模型解耦，可以适配不同 Agent 平台
- **人机共读**：知识文件本身即可被开发者阅读、编辑，也可以直接使用 Obsidian 等工具管理

其本质可以理解为：

```text
Agent
  │
  ├── 代码 / 项目文件
  ├── Skills / Rules
  └── Knowledge Base
          └── Markdown
```

知识库是 **Agent 工作环境的一部分**。

#### 形态二：独立知识系统

另一种路径是将 LLM-Wiki 从 Agent 的内部能力中拆出来，建设成一个**独立运行的知识系统**。

系统自己负责文档解析、索引、检索、知识关系维护等能力，并通过 UI、API 或 MCP 等接口向人类和外部 Agent 提供服务。

其典型架构是：

```text
              ┌──────────────┐
              │   Desktop UI │
              │    Web UI    │
              └──────┬───────┘
                     │
              ┌──────▼───────┐
              │ Knowledge     │
              │   System      │
              ├───────────────┤
              │ 文档解析       │
              │ 索引 / 检索    │
              │ 知识关系       │
              │ 向量搜索       │
              └──────┬────────┘
                     │
              ┌──────▼───────┐
              │ API / MCP     │
              └──────┬────────┘
                     │
             ┌───────▼────────┐
             │ External Agent │
             └────────────────┘
```

这种模式的核心特点是：

- **知识服务独立化**：知识库拥有自己的解析、索引、检索和管理能力
- **多数据源支持**：可以处理 Markdown 之外的 PDF、DOCX、PPTX、网页等内容
- **可视化管理**：可以提供知识树、全文搜索、关系图谱、文档预览等 UI
- **Agent 解耦**：多个 Agent 可以通过 API / MCP 共享同一套知识库
- **能力更完整**：适合规模更大的个人知识库或团队知识系统

`llm_wiki` 桌面应用就是这种形态的一种具体实现：它使用 Tauri 构建桌面 UI，通过 Rust 完成文件解析、搜索与图谱计算，并提供本地 API 供外部 Agent 调用。

------

因此，两种形态真正的区别并不是「Skill 还是桌面应用」，而是：

```text
                LLM-Wiki
                    │
          ┌─────────┴─────────┐
          ▼                   ▼
     Agent 内嵌式          独立知识系统
          │                   │
     Knowledge =           Knowledge =
     Agent 的一部分         独立基础设施
          │                   │
    Markdown / Skill       UI + Index
                           + API / MCP
```

前者强调的是**让 Agent 自己拥有知识**，后者强调的是**把知识能力建设成 Agent 可以调用的服务**。



### AGENTS.md：项目的"宪法"

`AGENTS.md` 是项目根目录下的 Markdown 文件，是 Agent 的**行为准则**。它告诉 Agent：这个项目是什么、用什么技术栈、有哪些约定、什么能做什么不能做。

📝 AGENTS.md 的核心内容

**项目身份**：这是什么项目、用什么语言/框架
**行为规则**：代码风格、提交规范、测试要求
**安全红线**：不能删除的数据、不能修改的配置
**工具偏好**：用 `pnpm` 不用 `npm`，用 `trash` 不用 `rm`
**环境信息**：Node 版本、Python 版本、数据库类型
**工作流程**：先跑测试再提交、先建分支再开发



### SOUL.md：Agent 的人格

`SOUL.md` 定义 Agent 的**人格、语气、交互风格**。它是 LLM-Wiki 体系中最"软"的一层——不涉及技术规则，而是塑造 Agent 的"性格"。

```markdown
# SOUL.md

你是一个务实、高效的工程助手。

## 语气
- 直接给方案，不废话
- 用代码说话，不用文字描述代码
- 犯了错就认，不找借口

## 风格
- 先问"要解决什么问题"，再给方案
- 涉及破坏性操作必须确认
- 不确定的事情说"不确定"，不编造
```

SOUL.md 在 Agent 框架中通常作为 System Prompt 的第一段注入，影响 Agent 的所有后续交互。Claude Code 的 System Prompt 分区设计中，SOUL.md 对应静态全局段——内容固定，可被 prompt cache 缓存。

一个完整的 LLM-Wiki 知识库遵循标准化的目录结构。以下是基于 llm-wiki-skill 和 llm_wiki 桌面应用的工业级实践：

Code📋

```code
my-wiki/
├── purpose.md              # 研究方向：目标、关键问题、研究范围
├── schema.md               # Wiki 结构规则：页面类型、分类方式
├── index.md                # 内容目录（LLM 导航入口）
├── log.md                  # 操作历史（可解析的时序记录）
├── overview.md             # 全局概要（每次 ingest 后自动更新）
├── raw/                    # 原始素材（不可变）
│   ├── articles/           # 网页文章
│   ├── tweets/             # X/Twitter
│   ├── wechat/             # 微信公众号
│   ├── pdfs/               # PDF
│   ├── notes/              # 笔记
│   └── assets/             # 附件
├── wiki/                   # AI 生成的知识库
│   ├── entities/           # 实体页：人物、组织、产品
│   ├── topics/             # 主题页：理论、方法、技术
│   ├── sources/            # 资料摘要页
│   ├── comparisons/        # 对比分析
│   ├── synthesis/          # 跨资料综合分析
│   │   └── sessions/       # 对话转知识页面
│   └── queries/            # 保存的查询结果
├── .wiki-schema.md         # 配置文件
└── .wiki-cache.json        # SHA256 去重缓存
```

🔑 关键设计决策

**index.md**：内容目录和 LLM 导航入口。LLM 每次查询时先读 index.md 了解知识库有哪些页面，再决定读哪些详情。

**log.md**：时序操作记录，可解析格式。记录每次 ingest/query/lint 操作，便于审计和回溯。

**overview.md**：全局概要页面，每次 ingest 后自动重新生成，反映 Wiki 最新状态。

**[[wikilink]]**：双向链接语法，用于页面间交叉引用。Obsidian 兼容。

**YAML frontmatter**：每个 Wiki 页面都有元数据头，包含 type、title、sources[] 等字段，支持结构化查询。

### purpose.md：Wiki 的灵魂

Karpathy 的原始设计有 Schema（Wiki 如何运作），但没有正式定义**为什么**这个 Wiki 存在。工业级实现新增了 `purpose.md`：

- 定义目标、关键问题、研究范围、演进中的论点
- LLM 在每次 ingest 和 query 时都会读取它以获取上下文
- LLM 可以根据使用模式建议更新
- 与 schema 不同——schema 是结构规则，purpose 是方向意图

### 知识图谱与关联度模型

Karpathy 的原始设计提到了 `[[wikilinks]]` 用于交叉引用，但没有图分析。工业级实现（llm_wiki 桌面应用）构建了完整的知识图谱可视化和关联度引擎。

### 四信号关联度模型

| 信号            | 权重 | 描述                                                   |
| :-------------- | :--- | :----------------------------------------------------- |
| **直接链接**    | ×3.0 | 通过 `[[wikilinks]]` 链接的页面                        |
| **来源重叠**    | ×4.0 | 共享同一原始资料的页面（通过 frontmatter `sources[]`） |
| **Adamic-Adar** | ×1.5 | 共享共同邻居的页面（按邻居度数加权）                   |
| **类型亲和**    | ×1.0 | 相同页面类型的加分（实体↔实体，概念↔概念）             |

这四个信号的加权组合形成一个综合关联度分数，用于图谱可视化中的边粗细和查询检索中的页面排序。



### 多阶段检索管线

📊 各阶段详解

**阶段 1：分词搜索** — 英文分词 + 停用词过滤；中文 CJK 二元组分词（每个 → [每个, 个…]）；标题匹配加分（+10）；同时搜索 wiki/ 和 raw/sources/

**阶段 1.5：向量语义搜索（可选）** — 通过任意 OpenAI 兼容的 /v1/embeddings 端点生成 embedding；存储在 LanceDB（Rust 后端）中快速 ANN 检索；余弦相似度发现语义相关页面即使没有关键词重叠；结果合并增强已有匹配 + 添加新发现

**阶段 2：图谱扩展** — 搜索结果作为种子节点；四信号关联度模型发现相关页面；2 跳遍历带衰减发现更深层关联

**阶段 3：预算控制** — 可配置上下文窗口 4K→1M tokens；比例分配：60% Wiki 页面，20% 聊天历史，5% 索引，15% 系统提示；页面按搜索 + 图谱关联度综合分数排序

**阶段 4：上下文组装** — 编号页面附完整内容（非仅摘要）；系统提示包含 purpose.md、语言规则、引用格式、index.md；LLM 被指示按编号引用页面：[1]、[2] 等



### Lint：知识库健康检查

定期运行 Lint 检查知识库健康度：

- **孤立页面**：没有任何双向链接的页面
- **断链**：指向不存在页面的 `[[wikilink]]`
- **索引一致性**：index.md 与实际文件是否匹配
- **AI 级矛盾检测**：不同页面间的事实矛盾
- **置信度标注**：EXTRACTED / INFERRED / AMBIGUOUS / UNVERIFIED

### 本地 API 与 Agent 集成

llm_wiki 桌面应用内置了本地 HTTP API（监听 `127.0.0.1:19828`，Token 鉴权，仅本机可达），让外部 AI Agent 可以直接查询知识库：

Code📋

```code
# 健康检查（无需鉴权）
GET /api/v1/health

# 项目列表
GET /api/v1/projects

# 读取文件树与内容
GET /api/v1/projects/{id}/files
GET /api/v1/projects/{id}/files/content

# Hybrid 混合检索（关键词 + 向量）
POST /api/v1/projects/{id}/search

# 非流式 Agent 聊天（支持 Wiki/Source/Web 检索）
POST /api/v1/projects/{id}/chat

# 知识图谱
GET /api/v1/projects/{id}/graph

# 触发资料源重新扫描
POST /api/v1/projects/{id}/sources/rescan
```

对于兼容 MCP 的客户端，还内置了 MCP Server，Agent 可以直接列出项目、读取文件、执行 Hybrid 检索、查看图谱、触发资料源重新扫描，不需要手写 HTTP 调用

### 隐私边界设计

llm-wiki 的隐私边界设计值得参考：

- **知识库文件和离线图谱输出**：始终留在本地机器
- **当请求 Agent 回答/摄入/生成内容时**：prompts、选中引用、检索片段、工具输出和生成产物可能发送到配置的模型提供商
- **API 密钥不存储在 llm-wiki 自身配置中**：第三方 Skills 仅在用户明确安装和启用后作为可信本地代码运行
- **本地工作台访问保护**：本地内容和状态变更需要同时通过来源检查和启动凭据检查，不受信任的网页无法读取或修改



## 🔑 八股卡

> 教程自带的「📋 八股总结」「📋 补充八股题」原文，**整段贴进来**。
> **想加强的那几条，用 `==……==` 把标题包起来。**

Q4：什么是两步思维链摄入（Two-Step Chain-of-Thought Ingest）？为什么要拆成两步？

Karpathy 原始设计是单步摄入——LLM 同时阅读和写入。工业级实现拆分为两步：Step 1（分析）LLM 阅读素材输出结构化分析 JSON（关键实体、核心论点、与现有 Wiki 的关联、矛盾、结构建议）；Step 2（生成）LLM 基于分析结果生成 Wiki 文件（资料摘要页、实体页、概念页、索引更新）。拆分的好处：① 分析和生成是不同认知任务，分离后各自质量更高；② 分析结果可被缓存和审查；③ 支持长内容的链式思考。工程上还配合 SHA256 增量缓存和持久化队列。





---

## 🧩 面试题（原题 + 我的答案）

> **只记做错的题**，对的不用抄。
> 记法：`~~X~~ Y` = 我选 X（错），正确答案是 Y。









---

## ❓ 疑问 / 待查

> 汇总本章没解决的疑问（正文里就地标过的，这里可再列一次，方便统一解决）。

- [ ] （疑问）



---

## 🔗 关联

- **前置**：
- **后续**：